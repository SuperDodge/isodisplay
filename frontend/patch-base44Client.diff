--- a/src/api/base44Client.js
+++ b/src/api/base44Client.js
@@
-import { createClient } from '@base44/sdk';
-// import { getAccessToken } from '@base44/sdk/utils/auth-utils';
-
-// Create a client with authentication required
-export const base44 = createClient({
-  appId: "6894c0eca3579b0fbcb94b66", 
-  requiresAuth: true // Ensure authentication is required for all operations
-});
+import { createClient } from '@base44/sdk';
+
+// Prefer build-time env var; fallback to placeholder to avoid leaking production IDs
+const appId = import.meta.env.VITE_BASE44_APP_ID ?? "CHANGE_ME";
+
+export const base44 = createClient({
+  appId,
+  requiresAuth: true
+});
